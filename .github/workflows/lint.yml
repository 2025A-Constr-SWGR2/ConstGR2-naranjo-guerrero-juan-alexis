name: Lint
run-name: Ejecutar lint
on: [push, pull_request]

jobs:
  lint:
    runs-on: [self-hosted]
    steps:
      - name: Descargar el repositorio
        uses: actions/checkout@v4

      - name: Usar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'

      - name: Instalar dependencias
        run: |
          cd calculadora
          npm install

      - name: Ejecutar lint
        run: |
          cd calculadora
          npm run lint